#ifndef DISCO_PLM_SOA2_H
#define DISCO_PLM_SOA2_H

#include "header.h"

double minmod_soa2(double a, double b, double c);
void plm_phi_soa2(struct domain *theDomain);
void plm_r_soa2(struct domain *theDomain);

#endif
